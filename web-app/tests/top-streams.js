import expect from "chai";

describe("List of Top Streams", () => {
    it("should display a list of tops streams.", () => {
        // given, when, then (assertion).
        // get React called, get DOM result, check the DOM.
        // need fake browser (JS DOM).

    })
});